/* istanbul ignore next */'use strict'; /* eslint-env jest */
jest.unmock('../SwitchTrack');

var /* istanbul ignore next */_react = require('react'); /* istanbul ignore next */var _react2 = _interopRequireDefault(_react);
var /* istanbul ignore next */_reactDom = require('react-dom');
var /* istanbul ignore next */_reactAddonsTestUtils = require('react-addons-test-utils');



var /* istanbul ignore next */_SwitchTrack = require('../SwitchTrack'); /* istanbul ignore next */var _SwitchTrack2 = _interopRequireDefault(_SwitchTrack);function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}

describe('SwitchTrack', function () {
  it('merges className and style', function () {
    var props = {
      style: { background: 'black' },
      className: 'test' };


    var track = /* istanbul ignore next */(0, _reactAddonsTestUtils.renderIntoDocument)( /* istanbul ignore next */_react2.default.createElement( /* istanbul ignore next */_SwitchTrack2.default, props));

    var trackNode = /* istanbul ignore next */(0, _reactDom.findDOMNode)(track);
    expect(trackNode.style.background).toBe(props.style.background);
    expect(trackNode.className).toContain(props.className);
  });

  it('adds the correct stateful classnames', function () {
    var props = { disabled: false, checked: false };
    var track = /* istanbul ignore next */(0, _reactAddonsTestUtils.renderIntoDocument)( /* istanbul ignore next */_react2.default.createElement( /* istanbul ignore next */_SwitchTrack2.default, props));
    var className = /* istanbul ignore next */(0, _reactDom.findDOMNode)(track).className;

    expect(className).toContain('md-switch-track');
    expect(className).toContain('md-pointer--hover');
    expect(className).not.toContain('--disabled');
    expect(className).not.toContain('--on');
    expect(className).toContain('--off');

    props.disabled = true;
    track = /* istanbul ignore next */(0, _reactAddonsTestUtils.renderIntoDocument)( /* istanbul ignore next */_react2.default.createElement( /* istanbul ignore next */_SwitchTrack2.default, props));
    className = /* istanbul ignore next */(0, _reactDom.findDOMNode)(track).className;

    expect(className).toContain('md-switch-track');
    expect(className).not.toContain('md-pointer--hover');
    expect(className).toContain('--disabled');
    expect(className).not.toContain('--on');
    expect(className).toContain('--off');

    props.checked = true;
    track = /* istanbul ignore next */(0, _reactAddonsTestUtils.renderIntoDocument)( /* istanbul ignore next */_react2.default.createElement( /* istanbul ignore next */_SwitchTrack2.default, props));
    className = /* istanbul ignore next */(0, _reactDom.findDOMNode)(track).className;

    expect(className).toContain('md-switch-track');
    expect(className).not.toContain('md-pointer--hover');
    expect(className).toContain('--disabled');
    expect(className).toContain('--on');
    expect(className).not.toContain('--off');
  });
});