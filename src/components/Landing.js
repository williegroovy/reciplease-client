import React from 'react';

export default () => (
  <div className="container">
    <h3>Welcome to Demos</h3>
    <p>Sign up to be logged in, or use <code>demo | pass</code> as a generic login</p>
  </div>);